export const SIMULATED_LOADING_TIME_IN_MS = 1500;

const serverURL = 'http://localhost:5000';

export const apiURL = {
  navigation: `${serverURL}/navigation`,
  companies: `${serverURL}/companies`,
  employees: `${serverURL}/employees`,
  projects: `${serverURL}/projects`,
};
